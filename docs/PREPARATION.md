> 履歴資料：この事前準備制限は、2026-09-15のユーザーによる本体移植・Slack機能追加の許可前のもの。現在の範囲はAGENTS.mdとMIGRATION.mdを参照。

# 事前準備の範囲

## 2026-09-15の作業範囲更新

ユーザーの明示的な依頼により、`add.minobe` ブランチで既存SlidePacerの実装・テストを統合し、ローカル検証を行う範囲を追加しました。[統合概要](SLIDEPACER_INTEGRATION.md)を参照してください。大会からの追加指示を確認したという意味ではありません。以下は当初の事前準備方針で、今回の統合対象以外に引き続き適用します。

## 当初の範囲

提供された「OpenAI 大学生向け 夏の Codex 開発祭 2026」資料33ページを根拠とする。企画のMarkdown化、不確定要素の整理、画像によるモック、必要環境の準備、GitHubリポジトリの基本設定・ワークフロー作成は準備例として記載されている。最終プロダクトのプロトタイプへの着手は避ける。

このリポジトリでは、設計資料・基本ルール・空の仕様書テンプレート・準備用ワークフローまでを対象とする。大会の追加指示があれば確認して反映する。

事前に含めないもの: 既存の製品コード、動作するモック、既存テスト、バイナリ、旧git履歴、作業記録、旧ファイル一覧、個人情報、認証情報。

本番開始は人間の明示的な指示を受ける。事前準備から本番へ切り替える際に、各機能の仕様書、最小実装範囲、担当、テスト計画を確認する。

2026-09-15のユーザー依頼により、過去の作業内容は現在の設計判断と検証状況の要約としてdocs/HANDOFF.mdに集約した。上記の「作業記録を含めない」は旧ログや旧リポジトリのファイル一覧の持ち込みを指し、今回許可された整理済みの引き継ぎ資料を妨げない。提出物の追加案内はdocs/SUBMISSION.mdを参照する。
