# 配布構成

設定手順は[infrastructure/SETUP.md](../infrastructure/SETUP.md)。必要Secret/変数はexport時に生成される `infrastructure/github-settings.json` を参照する（このテンプレート単体には未生成）。
新リポジトリのCDは未接続です。CIと署名、App ID、テスター、ビルド番号を確認してから有効化します。
