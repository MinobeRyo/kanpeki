name: Mac TestFlight CD (retired)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  use-unified-release:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Run TestFlight CD to release iPhone and Mac together.' >> "$GITHUB_STEP_SUMMARY"
